# kreativDental Plus - System Architecture

## 🏗️ Application Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         App.tsx (Root)                          │
│  - Manages authentication state                                 │
│  - Routes between Login and Main Layout                         │
│  - Provides Toaster for notifications                          │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                ┌─────────┴─────────┐
                │                   │
     ┌──────────▼─────────┐ ┌──────▼──────────┐
     │  LoginPage.tsx     │ │ MainLayout.tsx  │
     │                    │ │                 │
     │ - Employee ID      │ │ - Role-based    │
     │ - 6-digit passcode │ │   navigation    │
     │ - Authenticates    │ │ - User profile  │
     │ - Returns User obj │ │ - Routes pages  │
     └──────────┬─────────┘ └────────┬────────┘
                │                    │
     ┌──────────▼─────────┐          │
     │   data/users.ts    │          │
     │                    │          │
     │ - User database    │          │
     │ - Auth functions   │          │
     │ - 10 demo users    │          │
     └────────────────────┘          │
                          ┌──────────▼────────────┐
                          │   Role-Based Router   │
                          └──────────┬────────────┘
                                     │
         ┌───────────────────────────┼───────────────────────────┐
         │                           │                           │
    ┌────▼────┐              ┌───────▼────────┐         ┌───────▼────────┐
    │  Admin  │              │    Dentist     │         │     Staff      │
    │Dashboard│              │   Dashboard    │         │   Dashboard    │
    └─────────┘              └────────────────┘         └────────────────┘
         │                           │                           │
    ┌────▼────────┐          ┌──────▼─────────┐        ┌────────▼────────┐
    │Receptionist │          │ Standard Pages │        │  kreativPayroll │
    │ Dashboard   │          │                │        │  (All Roles)    │
    └─────────────┘          │ - Appointments │        └─────────────────┘
                             │ - Patients     │
                             │ - Financial    │
                             │ - Services     │
                             │ - etc.         │
                             └────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│  User Opens  │
│  Application │
└──────┬───────┘
       │
       ▼
┌─────────────────────────┐
│  LoginPage.tsx Loads    │
│  - Show Employee ID     │
│  - Show Passcode input  │
│  - Display demo creds   │
└──────┬──────────────────┘
       │
       ▼ User submits form
┌──────────────────────────────────┐
│  authenticateUser() called       │
│  - Match Employee ID             │
│  - Verify passcode               │
│  - Check expiration              │
└──────┬───────────────────────────┘
       │
       ├─── Success ───┐
       │               │
       ▼               ▼
┌──────────────┐  ┌──────────────────────┐
│ Update Last  │  │  Toast Success       │
│   Login      │  │  "Welcome back..."   │
└──────┬───────┘  └──────────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Return User Object     │
│  - employeeId           │
│  - name                 │
│  - role                 │
│  - position             │
│  - etc.                 │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  App.tsx receives user  │
│  - Set currentUser      │
│  - Set isLoggedIn=true  │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│  Render MainLayout      │
│  - Pass currentUser     │
│  - Show role dashboard  │
└─────────────────────────┘
```

---

## 👥 User Role Hierarchy

```
┌────────────────────────────────────────────────┐
│                    ADMIN                       │
│              (ADM-001)                         │
│  ✅ Full System Access                         │
│  ✅ All 9 Navigation Items                     │
│  ✅ Manage All Users                           │
│  ✅ Financial Access                           │
│  ✅ Inventory Management                       │
└────────────────┬───────────────────────────────┘
                 │
    ┌────────────┼────────────┐
    │            │            │
┌───▼─────┐ ┌───▼─────┐ ┌───▼─────────┐
│ DENTIST │ │  STAFF  │ │ RECEPTIONIST│
│(DEN-XXX)│ │(STF-XXX)│ │  (REC-XXX)  │
└────┬────┘ └────┬────┘ └──────┬──────┘
     │           │              │
     ▼           ▼              ▼
6 Nav Items  4 Nav Items   5 Nav Items
Personal     Personal      Front Desk
Dashboard    Dashboard     Dashboard
```

---

## 📊 Dashboard Architecture

```
┌─────────────────────────────────────────────────┐
│            MainLayout.tsx                       │
│  - Renders sidebar with user info               │
│  - Filters navigation by role                   │
│  - Routes to correct dashboard                  │
└──────────────────┬──────────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    ▼              ▼              ▼
┌───────────┐ ┌────────────┐ ┌─────────────┐
│   Admin   │ │  Dentist   │ │    Staff    │
│ Dashboard │ │ Dashboard  │ │  Dashboard  │
└───────────┘ └────────────┘ └─────────────┘
    │              │              │
    ▼              ▼              ▼
┌───────────────────────────────────────────┐
│        Component Structure:               │
│  - StatCard (metrics)                     │
│  - Activity/Schedule cards                │
│  - Quick action buttons                   │
│  - Role-specific data display             │
└───────────────────────────────────────────┘
```

---

## 🗂️ Data Structure

```
┌───────────────────────────────────────────────┐
│              data/users.ts                    │
│  ┌─────────────────────────────────────────┐ │
│  │ User Interface:                         │ │
│  │  - employeeId: string (PK)              │ │
│  │  - name: string                         │ │
│  │  - role: "admin"|"dentist"|"staff"|...  │ │
│  │  - passcode: string (to be hashed)      │ │
│  │  - position: string                     │ │
│  │  - email: string                        │ │
│  │  - mustChangePasscode: boolean          │ │
│  │  - passcodeSetDate: string              │ │
│  │  - lastLogin?: string                   │ │
│  └─────────────────────────────────────────┘ │
│                                               │
│  Functions:                                   │
│  - authenticateUser()                         │
│  - updateLastLogin()                          │
│  - changePasscode()                           │
│  - getUserByEmployeeId()                      │
└───────────────────────────────────────────────┘
```

---

## 🎨 Component Hierarchy

```
App.tsx
├── Toaster (sonner)
├── LoginPage.tsx
│   ├── Card
│   │   ├── CardHeader
│   │   ├── CardContent
│   │   │   ├── Alert (error display)
│   │   │   ├── Form
│   │   │   │   ├── Input (Employee ID)
│   │   │   │   └── Input (Passcode)
│   │   │   └── Demo Credentials Grid
│   └── Footer
│
└── MainLayout.tsx
    ├── Sidebar
    │   ├── Logo Section
    │   ├── User Info Section
    │   │   ├── Avatar
    │   │   ├── Name & Badge
    │   │   └── Employee ID
    │   ├── Navigation Menu (role-filtered)
    │   └── Settings & Logout
    │
    └── Main Content Area
        ├── Header
        │   ├── Mobile Menu Button
        │   ├── Page Title
        │   └── User Avatar
        │
        └── Dynamic Content
            ├── AdminDashboard
            ├── DentistDashboard
            ├── StaffDashboard
            ├── ReceptionistDashboard
            ├── AppointmentPage
            ├── PatientRecordPage
            ├── FinancialPage
            ├── ServiceCatalogPage
            ├── KreativPayrollPage
            ├── DentistsPage
            ├── AttendancePage
            └── InventoryPage
```

---

## 🔄 State Management

```
┌────────────────────────────────────────────────┐
│             App.tsx (Root State)               │
│  ┌──────────────────────────────────────────┐ │
│  │ State:                                    │ │
│  │  - isLoggedIn: boolean                    │ │
│  │  - currentUser: User | null               │ │
│  │                                           │ │
│  │ Functions:                                │ │
│  │  - handleLogin(user: User)                │ │
│  │  - handleLogout()                         │ │
│  └──────────────────────────────────────────┘ │
└────────────────────────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        │                           │
        ▼                           ▼
┌────────────────┐         ┌────────────────────┐
│  LoginPage     │         │   MainLayout       │
│                │         │                    │
│  Local State:  │         │  Props:            │
│  - employeeId  │         │  - currentUser     │
│  - passcode    │         │  - onLogout        │
│  - isLoading   │         │                    │
│  - error       │         │  Local State:      │
└────────────────┘         │  - activeTab       │
                           │  - sidebarOpen     │
                           └────────────────────┘
```

---

## 🛡️ Security Architecture

```
┌─────────────────────────────────────────────────┐
│           Current Implementation               │
│  ✅ Employee ID validation                      │
│  ✅ Passcode length check (6 digits)            │
│  ✅ Role-based access control                   │
│  ✅ Navigation filtering by role                │
│  ✅ Input sanitization                          │
│  ✅ Passcode expiration tracking                │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│         Production Requirements                 │
│  ⚠️  Add bcrypt/argon2 hashing                  │
│  ⚠️  Implement JWT tokens                       │
│  ⚠️  Add rate limiting                          │
│  ⚠️  Enable 2FA for admins                      │
│  ⚠️  Add audit logging                          │
│  ⚠️  Implement CSRF protection                  │
│  ⚠️  Use HTTPS only                             │
└─────────────────────────────────────────────────┘
```

---

## 📱 Responsive Design

```
┌────────────────────────────────────────────┐
│          Mobile (< 1024px)                 │
│  - Hidden sidebar (hamburger menu)         │
│  - Full-width main content                 │
│  - Mobile overlay when sidebar open        │
│  - Touch-optimized buttons                 │
└────────────────────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────┐
│         Desktop (≥ 1024px)                 │
│  - Persistent sidebar (264px)              │
│  - Flex-1 main content area                │
│  - Desktop navigation                      │
│  - Full feature access                     │
└────────────────────────────────────────────┘
```

---

## 🎯 Navigation Routes

```
/dashboard
  ├── admin → AdminDashboard.tsx
  ├── dentist → DentistDashboard.tsx
  ├── staff → StaffDashboard.tsx
  └── receptionist → ReceptionistDashboard.tsx

/appointment → AppointmentPage.tsx
/dentists → DentistsPage.tsx (admin only)
/patient-record → PatientRecordPage.tsx
/financial → FinancialPage.tsx (admin only)
/service-catalog → ServiceCatalogPage.tsx
/kreativ-payroll → KreativPayrollPage.tsx
/attendance → AttendancePage.tsx
/inventory → InventoryPage.tsx (admin only)
```

---

## 🔌 Backend Integration Points

```
┌────────────────────────────────────────────────┐
│         Frontend (Current)                     │
│  - React Components                            │
│  - Local state management                      │
│  - Mock data in /data/* files                  │
└──────────────┬─────────────────────────────────┘
               │
               ▼ API Integration
┌────────────────────────────────────────────────┐
│         Backend (To Implement)                 │
│                                                │
│  Authentication:                               │
│  - POST /api/auth/login                        │
│  - POST /api/auth/logout                       │
│  - POST /api/auth/change-passcode              │
│                                                │
│  Users:                                        │
│  - GET /api/users/:employeeId                  │
│  - PUT /api/users/:employeeId                  │
│                                                │
│  Data Endpoints:                               │
│  - /api/appointments/*                         │
│  - /api/patients/*                             │
│  - /api/financial/*                            │
│  - /api/payroll/*                              │
│  - /api/services/*                             │
│  - /api/dentists/*                             │
│  - /api/staff/*                                │
│  - /api/attendance/*                           │
│  - /api/inventory/*                            │
└────────────────────────────────────────────────┘
```

---

*Architecture designed for easy backend integration*
*All components are ready for API connections*
